# Store every text file with LF line endings in the repository. Each
# contributor's own core.autocrlf setting decides what their checkout uses.
#
# What gets published does not depend on that choice: tsconfig's
# "newLine": "lf" makes the compiled output LF, including the ecr
# binary's shebang, whose line ending macOS and Linux are sensitive to.
* text=auto
